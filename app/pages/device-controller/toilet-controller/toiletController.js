angular.module('toiletControlModule')

  .controller('toiletControllerCtrl', [
    '$scope',
    '$state',
    'baseConfig',
    'checkVersionService',
    function ($scope,
              $state,
              baseConfig,
              checkVersionService) {
    }]);
